# LangSmith로 토큰과 비용을 모니터링하는 방법

## 단계 1: LangSmith 계정 생성 및 API 키 발급
### 1-1. 회원가입
1. https://smith.langchain.com 접속
2. 우측 상단 "Sign Up" 클릭
3. 이메일로 가입 (무료 티어 5,000 traces/월)

### 1-2. API 키 발급
1. 로그인 후 좌측 하단 **Settings** 클릭
2. **API Keys** 메뉴 선택
3. **Create API Key** 클릭
4. 키 이름 입력 (예: "my-project-key")
5. **Create** 클릭
6. 생성된 API 키 복사 (예: `lsv2_pt_...`)
   - ⚠️ 이 키는 다시 볼 수 없으니 안전한 곳에 저장!

## 단계 2: 환경변수 설정

### 2-1. .env 파일 생성

프로젝트 루트에 `.env` 파일 생성:

```bash
# .env
OPENAI_API_KEY=your-openai-api-key
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=lsv2_pt_your-langsmith-api-key
LANGCHAIN_PROJECT=travel-planner  # 프로젝트 이름 (원하는 대로)
```

### 2-2. python-dotenv 설치

```bash
pip install python-dotenv
```

## 단계 3: 코드 수정 (매우 간단!)

기존 코드에 **4줄만 추가**하면 됩니다:

```python
# ===== 추가 코드 시작 =====
import os
from dotenv import load_dotenv

# 환경변수 로드
load_dotenv()
# ===== 추가 코드 끝 =====

from langchain_core.prompts import ChatPromptTemplate
from langchain_openai import ChatOpenAI
from langchain_core.output_parsers import JsonOutputParser
from rich.console import Console
from rich.panel import Panel
import json

console = Console()

# 출력을 위한 JSON 스키마 정의
json_schema = """
{{
  "destination": "목적지",
  "duration": "기간",
  "overview": "여행 개요",
  "daily_plans": [
   {{
      "day": 1,
      "title": "제목",
      "morning": "오전 일정",
      "lunch": "점심 추천",
      "afternoon": "오후 일정",
      "dinner": "저녁 추천",
      "accommodation": "숙소 추천"
    }}
  ],
  "tips": ["팁1", "팁2"],
  "total_budget": "예상 총 비용"
}}
"""

# LLM 초기화
llm = ChatOpenAI(
    model="gpt-4o-mini", 
    temperature=0.5,
    model_kwargs={"response_format": {"type": "json_object"}}
)

# 프롬프트
prompt = ChatPromptTemplate.from_messages([
    ("system", f"""당신은 {{place}} 베테랑 여행 가이드입니다.
고객 최적의 {{place}} {{travel}} 일정 수립을 도와줍니다.

다음의 JSON 형식으로 응답하세요.
{json_schema}"""),
    ("human", "해당 장소 {place}의 {travel} 일정에 맞는 여행 계획을 제안해주세요.")
])

# Json 출력 파서 정의
output_parser = JsonOutputParser()

# 체인 구성
chain = prompt | llm | output_parser

# llm 호출 - 자동으로 LangSmith에 기록됨!
input_data = {
    "place": "서울",
    "travel": "3일"
}

console.print("[bold cyan]여행 계획 생성 중...[/bold cyan]")
result = chain.invoke(input_data)

# 결과 출력
console.print(Panel(
    json.dumps(result, indent=2, ensure_ascii=False),
    title="[bold green]여행 계획[/bold green]",
    border_style="green"
))
```

**끝입니다!** 코드 수정은 이게 전부입니다.

## 단계 4: 코드 실행

```bash
python your_script.py
```

실행하면 자동으로 LangSmith에 추적 데이터가 전송됩니다.

## 단계 5: LangSmith에서 확인

### 5-1. Traces 확인

1. https://smith.langchain.com 접속
2. 좌측 메뉴에서 **Projects** 클릭
3. `travel-planner` 프로젝트 선택
4. **Traces** 탭에서 실행 내역 확인

각 trace를 클릭하면 다음을 볼 수 있습니다:
- ✅ **토큰 사용량** (Input tokens, Output tokens)
- ✅ **비용** (Cost in USD)
- ✅ **지연 시간** (Latency)
- ✅ **전체 실행 흐름** (프롬프트 → LLM → 파싱)

### 5-2. 비용 대시보드 확인

1. 프로젝트 선택
2. 상단 탭에서 **Dashboard** 클릭
3. 다음 정보 확인:
   - 총 비용
   - 토큰 사용량
   - 평균 지연시간
   - 성공률

## 단계 6: 고급 기능 추가 (선택사항)

### 6-1. 메타데이터 추가

```python
# 더 자세한 추적을 위해 메타데이터 추가
result = chain.invoke(
    input_data,
    config={
        "metadata": {
            "user_id": "user-123",
            "version": "v1.0",
            "environment": "production"
        },
        "tags": ["travel", "seoul", "production"]
    }
)
```

### 6-2. 사용자 피드백 수집

```python
from langsmith import Client

# LangSmith 클라이언트
client = Client()

# 실행
result = chain.invoke(input_data)

# 나중에 피드백 추가 (예: 사용자가 좋아요를 누른 경우)
# run_id는 LangSmith UI에서 확인하거나 아래 방법으로 얻을 수 있음
from langsmith.run_helpers import get_current_run_tree

run_tree = get_current_run_tree()
if run_tree:
    run_id = run_tree.id
    
    # 피드백 추가
    client.create_feedback(
        run_id=run_id,
        key="user_rating",
        score=5,  # 1-5
        comment="매우 유용했습니다!"
    )
```

### 6-3. 커스텀 함수 추적

```python
from langsmith import traceable

@traceable(run_type="chain", name="여행계획생성")
def create_travel_plan(place: str, days: str):
    """커스텀 함수도 추적"""
    input_data = {
        "place": place,
        "travel": days
    }
    return chain.invoke(input_data)

# 사용
result = create_travel_plan("서울", "3일")
```

## 단계 7: 비용 분석 예제

### 7-1. 여러 도시 테스트 및 비용 비교

```python
import os
from dotenv import load_dotenv
from langchain_core.prompts import ChatPromptTemplate
from langchain_openai import ChatOpenAI
from langchain_core.output_parsers import JsonOutputParser
from rich.console import Console
from rich.table import Table
import json

load_dotenv()
console = Console()

# 스키마
json_schema = """
{{
  "destination": "목적지",
  "duration": "기간",
  "overview": "여행 개요",
  "daily_plans": [
   {{
      "day": 1,
      "title": "제목",
      "morning": "오전 일정",
      "lunch": "점심 추천",
      "afternoon": "오후 일정",
      "dinner": "저녁 추천",
      "accommodation": "숙소 추천"
    }}
  ],
  "tips": ["팁1", "팁2"],
  "total_budget": "예상 총 비용"
}}
"""

# LLM
llm = ChatOpenAI(
    model="gpt-4o-mini",
    temperature=0.5,
    model_kwargs={"response_format": {"type": "json_object"}}
)

# 프롬프트
prompt = ChatPromptTemplate.from_messages([
    ("system", f"""당신은 {{place}} 베테랑 여행 가이드입니다.
고객 최적의 {{place}} {{travel}} 일정 수립을 도와줍니다.

다음의 JSON 형식으로 응답하세요.
{json_schema}"""),
    ("human", "해당 장소 {place}의 {travel} 일정에 맞는 여행 계획을 제안해주세요.")
])

output_parser = JsonOutputParser()
chain = prompt | llm | output_parser

# 여러 도시 테스트
cities = [
    ("서울", "3일"),
    ("부산", "2일"),
    ("제주", "4일"),
    ("경주", "2일"),
    ("강릉", "3일")
]

console.print("[bold cyan]여러 도시 여행 계획 생성 중...[/bold cyan]\n")

for place, days in cities:
    result = chain.invoke(
        {"place": place, "travel": days},
        config={
            "metadata": {
                "city": place,
                "duration": days,
                "test_type": "cost_comparison"
            },
            "tags": ["cost-test", place]
        }
    )
    console.print(f"✓ {place} {days} 완료")

console.print("\n[bold green]완료! LangSmith에서 비용을 확인하세요.[/bold green]")
console.print("URL: https://smith.langchain.com")
```

### 7-2. LangSmith에서 비용 필터링

실행 후 LangSmith에서:
1. **Filters** 클릭
2. **Metadata** → `test_type` = `cost_comparison` 선택
3. 우측 상단에서 **Total Cost** 확인
4. 각 도시별로 **Tags**로 필터링하여 비교

## 완전한 실전 예제 코드

```python
import os
from dotenv import load_dotenv
from langchain_core.prompts import ChatPromptTemplate
from langchain_openai import ChatOpenAI
from langchain_core.output_parsers import JsonOutputParser
from langsmith import Client
from rich.console import Console
from rich.panel import Panel
from rich.table import Table
import json

# ===== 1. 환경 설정 =====
load_dotenv()
console = Console()
langsmith_client = Client()

# ===== 2. JSON 스키마 =====
json_schema = """
{{
  "destination": "목적지",
  "duration": "기간",
  "overview": "여행 개요",
  "daily_plans": [
   {{
      "day": 1,
      "title": "제목",
      "morning": "오전 일정",
      "lunch": "점심 추천",
      "afternoon": "오후 일정",
      "dinner": "저녁 추천",
      "accommodation": "숙소 추천"
    }}
  ],
  "tips": ["팁1", "팁2"],
  "total_budget": "예상 총 비용"
}}
"""

# ===== 3. LLM 체인 구성 =====
llm = ChatOpenAI(
    model="gpt-4o-mini", 
    temperature=0.5,
    model_kwargs={"response_format": {"type": "json_object"}}
)

prompt = ChatPromptTemplate.from_messages([
    ("system", f"""당신은 {{place}} 베테랑 여행 가이드입니다.
고객 최적의 {{place}} {{travel}} 일정 수립을 도와줍니다.

다음의 JSON 형식으로 응답하세요.
{json_schema}"""),
    ("human", "해당 장소 {place}의 {travel} 일정에 맞는 여행 계획을 제안해주세요.")
])

output_parser = JsonOutputParser()
chain = prompt | llm | output_parser

# ===== 4. 실행 및 모니터링 =====
def create_travel_plan_with_monitoring(place: str, days: str, user_id: str = None):
    """모니터링이 포함된 여행 계획 생성"""
    
    console.print(f"[bold cyan]📍 {place} {days} 여행 계획 생성 중...[/bold cyan]")
    
    # LangSmith에 자동으로 기록됨
    result = chain.invoke(
        {"place": place, "travel": days},
        config={
            "metadata": {
                "user_id": user_id or "anonymous",
                "place": place,
                "duration": days,
                "version": "v1.0"
            },
            "tags": ["production", place, days]
        }
    )
    
    # 결과 출력
    console.print(Panel(
        json.dumps(result, indent=2, ensure_ascii=False),
        title=f"[bold green]{place} {days} 여행 계획[/bold green]",
        border_style="green"
    ))
    
    return result

# ===== 5. 테스트 실행 =====
if __name__ == "__main__":
    # 단일 실행
    result = create_travel_plan_with_monitoring("서울", "3일", "user-123")
    
    console.print("\n[bold green]✓ 완료![/bold green]")
    console.print("[bold yellow]LangSmith에서 확인하세요:[/bold yellow]")
    console.print(f"https://smith.langchain.com/projects")
    
    console.print("\n[bold cyan]확인 가능한 정보:[/bold cyan]")
    table = Table(show_header=True, header_style="bold magenta")
    table.add_column("항목", style="cyan")
    table.add_column("설명", style="green")
    
    table.add_row("토큰 사용량", "Input/Output 토큰 수")
    table.add_row("비용", "USD 단위 비용")
    table.add_row("지연 시간", "응답 시간 (ms)")
    table.add_row("성공률", "성공/실패 비율")
    table.add_row("실행 흐름", "프롬프트 → LLM → 파싱")
    
    console.print(table)
```

## 체크리스트

- [ ] LangSmith 계정 생성
- [ ] API 키 발급 및 저장
- [ ] `.env` 파일에 환경변수 설정
- [ ] `python-dotenv` 설치
- [ ] 코드에 `load_dotenv()` 추가
- [ ] 코드 실행
- [ ] LangSmith 웹사이트에서 확인

## 주요 확인 사항

### LangSmith UI에서 확인할 수 있는 것들:

1. **Traces 탭**
   - 각 실행의 상세 정보
   - 토큰 사용량 (input/output)
   - 비용 (USD)
   - 지연시간

2. **Dashboard 탭**
   - 총 실행 횟수
   - 총 비용
   - 평균 지연시간
   - 에러율

3. **Prompts 탭** (고급)
   - 프롬프트 버전 관리
   - A/B 테스트

## 문제 해결

### 추적이 안 되는 경우

```python
# 환경변수가 제대로 설정되었는지 확인
import os

print("LANGCHAIN_TRACING_V2:", os.getenv("LANGCHAIN_TRACING_V2"))
print("LANGCHAIN_API_KEY:", os.getenv("LANGCHAIN_API_KEY")[:10] + "...")
print("LANGCHAIN_PROJECT:", os.getenv("LANGCHAIN_PROJECT"))
```

### 수동으로 설정하는 방법

```python
import os

# .env 파일 없이 직접 설정
os.environ["LANGCHAIN_TRACING_V2"] = "true"
os.environ["LANGCHAIN_API_KEY"] = "lsv2_pt_..."
os.environ["LANGCHAIN_PROJECT"] = "travel-planner"
```

이제 코드를 실행하고 LangSmith에서 토큰 사용량과 비용을 실시간으로 모니터링할 수 있습니다! 🎉